{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "devCommand": "uvicorn cmd.main:app --reload --host 0.0.0.0 --port 8000",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
